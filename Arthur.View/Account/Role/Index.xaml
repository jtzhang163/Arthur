<UserControl x:Class="Arthur.View.Account.Role.Index"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Arthur.View.Account.Role"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" FontSize="30" Height="35" Text="角色列表"/>
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" FontSize="30" Height="35" HorizontalAlignment="Right"><Hyperlink x:Name="create" Click="create_Click">新增</Hyperlink></TextBlock>
                <ListView x:Name="listView">
                    
                </ListView>
            </DockPanel>
        </DockPanel>
    </Grid>
</UserControl>
